<!-- Main hero unit for a primary marketing message or call to action -->
<div class="hero-unit">
  <h1>Wer nicht fragt, bleibt dumm!</h1>
    <p>Stelle deine Frage an Uru und philosophiere mit Gleichgesinnten über eine mögliche Antwort.</p>
    <p><%= link_to new_question_path(@question), :class => "btn btn-tall" do %>
        <i class="icon-info-sign"></i> Stell deine Frage<% end %></p>
</div>

      <!-- Example row of columns -->
      
<div class="Zufallsbereich">
    <h2>Fragen auf Fraguru</h2>
    <% @questions = Question.random(3) %>
      <% @questions.each do |question| %>
        <div class="Frage">
          <p><%= question.message %></p>
          <p><%= link_to question_path(question), :class => "btn btn-small" do %>
          <i class="icon-info-sign"></i> Details<% end %>
          </p>
        </div>
      <% end %>
  </div>
</div>
