<p id="notice"><%= notice %></p>

<p>
  <b>Frage:</b><br />
  <%= @question.message %>
</p>
<div class="question-information">
  <%= "Frage erstellt von: #{@question.member.nickname}" %>
</div>

<%# = link_to pluralize(@question.replies.size, 'reply'), question_replies_path(@question) %>

<% @question.replies.each do |reply| %>
  <div class="reply">
    <div class="reply-message">
      <p><%= reply.message %></p>
    </div>
    <div class="question-information">
      <%= "Antwort erstellt von: #{reply.member.nickname}" %>
    </div>
    <div class="reply-action">
      <p>      
        <% if current_member == reply.member %>
	  <%= link_to edit_reply_path(@question), :class => "btn btn-small" do %>
	  <i class="icon-edit"></i> Bearbeiten<% end %> 
	  <%= link_to @question, :class => "btn btn-small", method: :delete, data: { confirm: 'Bist du sicher?' } do %>
	  <i class="icon-trash"></i> Löschen<% end %>
	<% end %>
      </p>
    </div>
  </div>
<hr>
<% end %>
-->

<%= link_to 'Bearbeiten', edit_question_path(@question) %> |
<%= link_to 'Zurück', questions_path %>
